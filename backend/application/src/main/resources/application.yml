spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/newsfeed
    username: postgres
    password: darkmilchocolate
  data:
    redis:
      host: localhost
      port: 6379
  flyway:
    enabled: true
    locations: classpath:db/migration
